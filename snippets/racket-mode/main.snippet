# -*- mode: snippet -*-
# name: main module
# key: main.
# --
${1:(: main (->* () ((Vectorof String)) ${2:Any}))
}(define main
  (λ ([argv (current-command-line-arguments)])
    (let/cc ${3:return} : $2
      $0
      ($3 ${4:0}))))
(module+ main (main))